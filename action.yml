name: "P6 GHA: Upgrade Dependencies for a CDK App"
description: "P6 GHA: Upgrade Dependencies for a CDK App"
author: "Philip M. Gollucci"
inputs:
  gh_token:
    description: "Automation token used to trigger auto-approve events (P6_A_GH_TOKEN)"
    required: true
runs:
  using: "composite"
  steps:
    - name: Checkout code
      uses: p6m7g8-actions/gh-repo-checkout@main
      with:
        fetch-depth: 0
        ref: main
    - name: Node
      uses: p6m7g8-actions/p6-node-setup@main
    - name: Update Dependencies
      shell: bash
      run: |
        pnpm up --latest
    - name: Get Dependency Diff
      shell: bash
      run: |
        git diff
    - name: Normalize GitHub token
      id: token
      uses: p6m7g8-actions/gh-token-resolve@main
      with:
        # gh-token-normalize returns the first non-empty slot. github.token is
        # always non-empty, so it must not sit in preferred-token or the
        # automation PAT can never win. PRs opened with GITHUB_TOKEN do not
        # trigger `pull_request` workflows, so those PRs would get no CI.
        preferred-token: ${{ inputs.gh_token }}
        fallback-token: ${{ github.token }}
        default-token: ${{ github.token }}
    - name: Create Pull Request
      id: create_pr
      uses: peter-evans/create-pull-request@v8.1.0
      with:
        token: ${{ steps.token.outputs.value }}
        branch-token: ${{ steps.token.outputs.value }}
        commit-message: |
          chore(deps): upgrade dependencies
        branch: github-actions/upgrade-main
        title: "chore(deps): upgrade dependencies"
        labels: |
          auto-approve
          auto-merge
        reviewers: |
          p6m7g8-automation
        body: |
          Upgrades project dependencies.
        author: github-actions <github-actions@github.com>
        committer: github-actions <github-actions@github.com>
        signoff: true
    - name: Re-label PR with automation token
      if: steps.create_pr.outputs.pull-request-number != ''
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.gh_token || github.token }}
        PR: ${{ steps.create_pr.outputs.pull-request-number }}
        REPO: ${{ github.repository }}
      run: |
        gh api -X DELETE "repos/${REPO}/issues/${PR}/labels/auto-approve" || true
        gh api "repos/${REPO}/issues/${PR}/labels" -f labels[]='auto-approve'
